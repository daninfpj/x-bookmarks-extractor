{
  "name": "x-bookmarks",
  "version": "1.0.0",
  "scripts": {
    "fetch": "bun run fetch-bookmarks.ts",
    "summarize": "bun run summarize-bookmarks.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.69"
  }
}
